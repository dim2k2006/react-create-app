import React, {Component} from 'react';
import './style.css';

class Uploader extends Component {
    render() {
        return (
            <div className="uploader">
                <div className="uploader__body"></div>
            </div>
        );
    }
}

export default Uploader;
